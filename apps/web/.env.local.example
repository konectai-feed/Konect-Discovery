# Base URL for the Konect Discovery backend API
# Used by the Next.js API proxy route (apps/web/src/app/api/search/route.ts)
KONECT_API_BASE_URL=https://konect-discovery.onrender.com

# SECURITY NOTE: Do NOT add SUPABASE_SERVICE_ROLE_KEY here.
# The frontend talks to the backend via the proxy above.
# Only the backend (services/api) should hold SUPABASE_SERVICE_ROLE_KEY.
# If you need Supabase directly in the browser, use NEXT_PUBLIC_SUPABASE_ANON_KEY
# and the anon key only — it is safe to expose publicly.
